Before copying the local Windows system to an Ubuntu system need to follow this steps of install
	https://nodejs.org/en/download/package-manager
	installs nvm (Node Version Manager)
	curl -o- https://raw.githubusercontent.com/nvm-sh/nvm/v0.40.0/install.sh | bash
	# download and install Node.js (you may need to restart the terminal)
	nvm install 22
	# verifies the right Node.js version is in the environment
	node -v # should print `v22.7.0`
	# verifies the right npm version is in the environment
	npm -v # should print `10.8.2`
To copy files from a local Windows system to an Ubuntu system, follow these steps:
	Windows Local file path (d/Dot\ net/CMS-Dashboard-master ~/CMS-Dashboard-master)
	cp -r /mnt/d/Dot\ net/CMS-Dashboard-master ~/CMS-Dashboard-master
	ls ~/CMS-Dashboard-master
	ionic serve
